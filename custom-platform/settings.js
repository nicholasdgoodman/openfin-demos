export default {
    snapMargin: 8,          // how close two edges must be to show the snap preview
    snapPeriod: 150,        // number of milliseconds between location polling
    snapSensistivity: 2     // number of periods a window must be in range to show preview window
}